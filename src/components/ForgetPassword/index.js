import { Link } from 'react-router-dom';
import styles from './ForgetPassword.module.scss'

const ForgetPassword = () => {
    return <a className={styles.forgetPassword} href="a">Forgot your password ?</a>;
};

export default ForgetPassword;
